#include<bits/stdc++.h>
using namespace std;
int main()
{
int t;
double p,time,m,b=1000000000,z=0.0;
cin>>t;
while(t--)
{
cin>>m>>p;
z=<b*pow(p,m-1);
if(m%2!=0)					//chef plays in the end..
cout<<z<<" "<<b-z<<"\n";
else 
cout<<b-z<<" "<<z<<"\n";
return 0;
}
